/**
 * Shared domain entity types for the MyRangeBC frontend.
 *
 * Types here are derived from what the frontend receives from the API,
 * not copied from the backend. They represent the frontend's view of
 * the domain model.
 */
